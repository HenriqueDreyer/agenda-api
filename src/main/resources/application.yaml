spring:
  docker:
    compose:
      enabled: true
      file: docker-compose.yaml
      
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/agenda_db    
  jpa:
    hibernate:
      ddl-auto: update

